package models;

import org.junit.Assert;
import org.junit.Test;

public class UserProfileTest {

 /*
    @Test
    public void userProfile() {
        UserProfile newUser = new UserProfile(null, null, null, null);
    }

    @Test
    public void getFirstName() {
        String givenFirstName = "Kane";

        UserProfile newUser = new UserProfile(givenFirstName, null, null, null);

        String actual = newUser.getFirstName();
        String expected = "Kane";

        Assert.assertEquals(expected, actual);
    }

    @Test
    public void getLastName() {
        String givenLastName = "Kopetski";

        UserProfile newUser = new UserProfile(null, givenLastName, null, null);

        String actual = newUser.getLastName();
        String expected = "Kopetski";

        Assert.assertEquals(expected, actual);
    }

    @Test
    public void getFullName() {
        String givenFirstName = "Kane";
        String givenLastName = "Kopetski";

        UserProfile newUser = new UserProfile(givenFirstName, givenLastName, null, null);

        String actual = newUser.getFullName();
        String expected = "Kane Kopetski";

        Assert.assertEquals(expected, actual);
    }

    @Test
    public void getuserName() {
        String givenUserName = "kkopetski";

        UserProfile newUser = new UserProfile(null, null, givenUserName, null);

        String actual = newUser.getUserName();
        String expected = "kkopetski";

        Assert.assertEquals(expected, actual);
    }

    @Test
    public void getPassword() {
        String givenPassword = "defaultPW1!";

        UserProfile newUser = new UserProfile(null, null, null, givenPassword);

        String actual = newUser.getPassword();
        String expected = "defaultPW1!";

        Assert.assertEquals(expected, actual);

    }

    @Test
    public void setFirstName() {
        String newFirstName = "Kane";

        UserProfile newUser = new UserProfile(null, null, null, null);

        newUser.setFirstName(newFirstName);

        String actual = newUser.getFirstName();
        String expected = "Kane";

        Assert.assertEquals(expected, actual);
    }

    @Test
    public void setLastName() {
        String newLastName = "Kopetski";

        UserProfile newUser = new UserProfile(null, null, null, null);

        newUser.setLastName(newLastName);

        String actual = newUser.getLastName();
        String expected = "Kopetski";

        Assert.assertEquals(expected, actual);
    }

    @Test
    public void setUserName() {
        String newUserName = "kkopetski";

        UserProfile newUser = new UserProfile(null, null, null, null);

        newUser.setUserName(newUserName);

        String actual = newUser.getUserName();
        String expected = "kkopetski";

        Assert.assertEquals(expected, actual);
    }

    @Test
    public void setPassword() {
        String newPasswordName = "defaultPW1!";

        UserProfile newUser = new UserProfile(null, null, null, null);

        newUser.setPassword(newPasswordName);

        String actual = newUser.getPassword();
        String expected = "defaultPW1!";

        Assert.assertEquals(expected, actual);
    }

  */
}